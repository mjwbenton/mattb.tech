{
  "name": "@mattb.tech/pdf-cv",
  "version": "1.0.0",
  "author": "Matt Benton <m@mattb.tech>",
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "prettier": "^2.6.2",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3"
  },
  "scripts": {
    "check": "../scripts/check.sh",
    "fix": "../scripts/fix.sh",
    "generate": "ts-node ./generate.ts",
    "check-exists": "[[ -f cv.pdf ]] && exit 0 || (echo 'cv.pdf not found' && exit 1)"
  },
  "dependencies": {
    "puppeteer": "^13.6.0"
  }
}
